//! Card 36 · Question, plan and todo cards. Stub: replaced by the port in this phase.

use gpui::*;

/// Builds the card content.
pub fn build(_window: &mut Window, _cx: &mut App) -> AnyElement {
    div().child("36 · Question, plan and todo cards — not ported yet").into_any_element()
}
