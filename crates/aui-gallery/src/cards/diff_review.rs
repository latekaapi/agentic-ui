//! Card 52 · Diff review. Stub: replaced by the port in this phase.

use gpui::*;

/// Builds the card content.
pub fn build(_window: &mut Window, _cx: &mut App) -> AnyElement {
    div().child("52 · Diff review — not ported yet").into_any_element()
}
