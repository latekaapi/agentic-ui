//! Card 53 · Git and PR. Stub: replaced by the port in this phase.

use gpui::*;

/// Builds the card content.
pub fn build(_window: &mut Window, _cx: &mut App) -> AnyElement {
    div().child("53 · Git and PR — not ported yet").into_any_element()
}
