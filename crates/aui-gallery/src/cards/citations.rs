//! Card 55 · Sources and citations. Stub: replaced by the port in this phase.

use gpui::*;

/// Builds the card content.
pub fn build(_window: &mut Window, _cx: &mut App) -> AnyElement {
    div().child("55 · Sources and citations — not ported yet").into_any_element()
}
