# Handoff prompt for the next implementation session

Paste the block below into a new Claude Code session opened in this repository.

```
Continue the implementation of the Agentic UI library in /Users/latekaapi/Projects/agentic-ui. Phases 1–3 are done and committed: workspace, aui-tokens (generated gpui-kit theme, bundled Geist, CSS-blur shadows), aui-icons, aui-protocol, aui-motion, and the `aui` component crate with every design card ported at parity — data primitives, shell (cards 10–13), sidebar (20–23), transcript (30–38), composer (40–42), workbench (50–55) — plus an assistant mock (`screens/assistant`, 1440×900, light) assembled live inside the gallery. `docs/03-parity-process.md` has the commit hash and differing-pixel fraction for every card. A bug-fix pass on 2026-09-06 (from the user's annotated screenshots) is in the working tree and is NOT committed yet: commit it first (`git status` shows ~35 files; message: "Phase 3 fixes: collapsed rail, popover layer, tint_fade hovers, uniform note cards"). That pass added: `AppShell::rail` + `traffic_lights` (collapsed column is 72 px with window controls, `SidebarHeader::collapsed`, `CentreHeader::on_expand_sidebar` is the reopen affordance, `AUI_GALLERY_COLLAPSED=1` renders screens collapsed); `aui::overlay::popover_layer` (a `gpui::deferred` wrapper every popover must use — gpui paints border after children and later siblings over anything that overflows); `aui_motion::tint_fade` (hover tints fade alpha only; never tween a colour to transparent); note cards are one plain hairline box with no accent rail or dashed variant (design source + references for cards 10/37/52 re-rendered); toast stack fans out above siblings with a hover pad. Still unverified by hand: live spring motion of the sidebar toggle, toast collapse and plus-menu morph, and light-theme renders of cards 37/52 — check these in the running gallery before Phase 4 step 2.

Read first, in this order: docs/00-agent-brief.md, your memory files (gpui-pre-api-notes, feedback-design-taste, project-decisions-2026-09-05), docs/04-design-rules.md, docs/03-parity-process.md (tooling, known gaps, checklist), docs/02-component-spec.md (§5.5–5.6 and §6 screens), README.md, crates/aui/src/lib.rs and the module roots (data/mod.rs, shell/mod.rs, nav/mod.rs, transcript/mod.rs, composer/mod.rs, workbench/mod.rs) for the public API, crates/aui-gallery/src/assistant/view.rs (the mock: how the components compose into an app), crates/aui-gallery/src/registry.rs.

The design is the contract: design/src/cards/*.html + design/src/base.css + design/tokens/tokens.css hold exact values; design/reference/{cards,screens}/*.png are what to match; docs/02 is corrected when the CSS disagrees; references are re-rendered with headless Chrome after any design change (command in docs/03). The screen references in design/reference/screens were rendered before the divider fix in design/screens/common.py and still show the staggered first divider; re-render them (design/screens/*/build_screens.py → the canvas seeds; the PNGs came from the design skill) before using them for parity. Screen canvases: https://claude.ai/code/artifact/e77554a4-ff0f-4eb6-ae2f-a838de61d384 (harness), https://claude.ai/code/artifact/bdc61083-243f-4dce-8b4f-9e65ff4dce7a (assistant).

Stack: Rust, gpui-pre 0.3.3 as `gpui` + gpui-kit 0.6 (registry sources under ~/.cargo/registry/src/index.crates.io-*/), macOS only. Prefix shell commands with `export PATH="/opt/homebrew/bin:$HOME/.cargo/bin:$PATH"`. Tools: `cargo run -p aui-gallery` (flags: --theme, --entry, --list, --screenshot <id> <png>, --screenshot-window <png>, --screenshot-delay <ms>, --text-scale), `scripts/parity.sh <entry-id> <reference-stem>` (≤2 % differing pixels is parity for a static card; parity renders open at the display's top-left and occlude the pointer), ImageMagick `compare`/`magick`. When subagents edit the shared tree, build your own work in a `git worktree` with its own CARGO_TARGET_DIR (see memory).

Conventions already decided: no literal colours/sizes/durations in components — `cx.aui().colors`, `aui_tokens::scale::*`, `cx.aui().metrics`, `AuiStyled::{text_role, ui, mono, text_px}` (product text scale 1.1; parity at 1.0); motion only through aui-motion; stateless `RenderOnce` components with lowercase constructors, per-element hover/press state via `aui::util::{interaction_flags, TrackInteraction}`, data in / intents out via closures, no I/O; light and dark both correct; mixed-font inline text via `StyledText::with_runs` (`aui::transcript::prose`, `ansi_runs`, `syntax_runs`); CSS margins collapse only in block flow (rows in a flex column keep both margins); gpui has no letter-spacing, no element transform, and treats `BoxShadow.blur_radius` as sigma (tokens already halve the CSS blur).

Phase 4 deliverables, in order:
1. Assistant mock completeness: match assistant-Main / Sources / Sheet within 2 % (remaining: composer chip order — the assistant puts the knowledge chip before the model chip; the Sources screen's `sources_card` + hover card in the transcript; the streaming caret measured to the last line via `window.text_system().shape_text` and captured bounds; the doc pane's in-shell tabs). Add a `screens` gallery page that shows the assistant screens side by side. The harness screens (harness-Main / DiffReview / TerminalMode / NewTask) can be assembled as a second mock only if time allows — the user dropped the mocked harness app.
2. Real behaviour behind the intents in the mock: composer send → user turn + streaming assistant turn (StreamReveal), approvals/questions resolving, right-pane open/close on the layout spring, sidebar rail (⌘B), command palette on ⌘K with focus restore, toasts entering/exiting, ViewMenu switching the grouping. Keyboard: Y/A/N on the approval card, ↑↓/↩/esc in palette and menus, tab stops and the accent focus ring on buttons.
3. Library hygiene: `docs/06-api.md` per-module API overview generated from rustdoc (`cargo doc --workspace --no-deps`), unit tests for pure helpers (prose parser, ANSI, syntax lexer, duration formatting, file-type classification), a `tab_strip` sliding thumb for the segmented control once a measured layout pass exists, `nav::chevron` size parameter, tree-sitter highlighting behind a feature flag if compile time allows.
4. Then aui-webview (wry/WKWebView pane behind `BrowserNav` with the annotator bridge) and aui-terminal (PTY behind `BlockTerminal` / `TuiPane`), each with a gallery entry driven by a fake backend first.

Token discipline (Fable is expensive; four fifths of its spend is re-reading context each turn): keep Fable to as few turns as possible. Do not load deferred tools or skills mid-session on Fable (each load re-caches the whole context); do all Fable tool loading in the first turn or hand it to an agent. Disable unused connectors for this project in the desktop app before starting (Chrome, iOS Simulator, HyperFrames, Drive, visualize, scheduled-tasks) so the tool list stops being re-sent every turn. For a batch of fixes or ports, brief ONE Opus lead agent with the whole batch and let it run Opus/Sonnet workers and review them; Fable wakes once at the end to spot-check cropped PNGs. Point agents at docs/00-agent-brief.md instead of docs/02 and docs/03 (they otherwise burn ~100 turns re-reading the spec). Give each parallel agent its own worktree and CARGO_TARGET_DIR so they stop waiting on the cargo lock. Fable reads screenshots (cheap) but does not build or run parity itself except one final check.

Rules: follow the parity loop for every card you touch (build, gallery entry at the card's size, scripts/parity.sh, compare with the reference, exercise every interaction and motion in the spec, tick docs/03 with the commit hash). Keep architecture, the motion engine, tricky gpui elements and all reviews on Fable; delegate routine ports, sample data, tests and docs to Opus 5 subagents (give them the exact conventions above, one file set each, forbid editing files outside their scope, run only `cargo build -p aui-gallery` / `cargo test -p <crate>`) and review their output before it lands — expect the shared build to break while they edit; keep a worktree for your own parity runs. Do not touch or wire into my existing assistant app. Commit at the end of each step with a clear message.
```

The session's persistent memory lives outside the repo at
`~/.claude/projects/-Users-latekaapi-Projects-agentic-ui/memory/`; `docs/memory-snapshot/` is a copy taken at handoff time.

## State at handoff (2026-09-06)

- 2026-09-06 bug-fix pass (uncommitted at handoff, see prompt): collapsed sidebar rail, reopen toggle and traffic lights; toast hover fan-out; composer plus-menu layering; note-card artifact and uniform note style; command palette / menu / nav / workbench hover fades. Parity for cards 10, 12, 20–23, 37, 40, 41, 52, 54 unchanged or slightly better; numbers updated in docs/03.

- 26 gallery entries at parity (worst: card 40 at 2.3 %, card 55 at 2.9 %; both glyph rasterisation), plus `screens/assistant` (3.8 % against `assistant-Main`).
- Design fixes made this phase (references re-rendered): continuous first divider (card 10 + screens/common.py), card 38 `.sm` class collision, card 50 tab indicator to ink/bottom, card 52 notes column line number, card 53 PR description block flow; sprite gained `spinner-ring`, `spinner-arc`, `check-bold`, `x-bold`, `chev`.
- Protocol additions: `Attachment.meta`, `ToolBody::Web.hidden`, `ToolBody::Browser.caption`, ANSI-coloured sample shell output, extra sample hunks.
- Known follow-ups are listed under "Known, accepted gaps" in docs/03 and in the phase-4 prompt above.
